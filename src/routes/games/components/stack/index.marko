class {
  onCreate(input) {
    this.state = {
      top: input.color,
      height: 1
    };
  }
}

style {
  .square {
    width: 50px;
    height: 50px;
  }
}

div.square style={backgroundColor: state.top}
